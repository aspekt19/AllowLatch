# Optional — only if you call OpenAI directly via process(). Prefer generate() / runless.
# OPENAI_API_KEY=

# Required for cloud deploy (from https://platform.openserv.ai dashboard)
# OPENSERV_USER_API_KEY=

# Auto-filled by provision()
WALLET_PRIVATE_KEY=
OPENSERV_API_KEY=
OPENSERV_AUTH_TOKEN=
PORT=7378

# Coinbase AgentKit / CDP (for real Base transfers)
# https://portal.cdp.coinbase.com
CDP_API_KEY_ID=
CDP_API_KEY_SECRET=
CDP_WALLET_SECRET=
# CDP_WALLET_ADDRESS=
NETWORK_ID=base-sepolia
# dry-run | live  (default: live if CDP_* set, else dry-run)
# MANDATEGUARD_EXECUTE_MODE=dry-run
