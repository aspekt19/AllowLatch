{
  "name": "AllowLatch",
  "version": "0.1.0",
  "description": "SERV Policy Copilot + spending turnstile for financial AI agents on Base (USDC). Natural-language mandates → strict policy → ALLOW/DENY/ESCALATE before AgentKit. Non-custodial. End users need no API keys.",
  "homepage": "https://allowlatch.vercel.app",
  "repository": "https://github.com/aspekt19/AllowLatch",
  "documentation": "https://github.com/aspekt19/AllowLatch/blob/main/docs/CONNECT.md",
  "product": "https://github.com/aspekt19/AllowLatch/blob/main/docs/PRODUCT.md",
  "protocol": {
    "openserv": {
      "agentName": "allowlatch",
      "workflowName": "AllowLatch",
      "discover": "PlatformClient.payments.discoverServices() — filter name AllowLatch",
      "invoke": "x402 payWorkflow with { prompt } — no end-user API keys"
    }
  },
  "skills": [
    {
      "id": "draft_policy",
      "description": "SERV: draft MandatePolicy with conflicts, assumptions, questions (Multipath, prompt_guard, shadow)."
    },
    {
      "id": "revise_mandate",
      "description": "SERV: revise policy from owner feedback."
    },
    {
      "id": "apply_policy",
      "description": "Persist an accepted MandatePolicy under a policyId."
    },
    {
      "id": "evaluate_intent",
      "description": "Deterministic allow | deny | escalate. No transaction."
    },
    {
      "id": "explain_decision",
      "description": "SERV: explain a gate result; never overrides the verdict."
    },
    {
      "id": "execute_gated_transfer",
      "description": "Evaluate then AgentKit USDC transfer only on ALLOW (or escalate + humanApproved)."
    }
  ],
  "invariants": [
    "Allow/deny/escalate is deterministic code — never LLM judgment",
    "AllowLatch does not custody user funds or private keys",
    "End users do not configure SERV_API_KEY or CDP — the host does; x402 pays the call"
  ],
  "stacks": ["Base", "USDC", "Coinbase AgentKit", "OpenServ", "SERV Reasoning"]
}
