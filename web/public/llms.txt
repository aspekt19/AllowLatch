# AllowLatch

> SERV Policy Copilot + spending turnstile for financial AI agents on Base/USDC.

## For AI agents (read this first)

You are helping a human who owns an AI agent with a wallet (e.g. AgentKit or similar). They will **not** give you API keys for AllowLatch.

### What to do

1. Treat AllowLatch as an **external hosted gate**: before any spend/sign, ask AllowLatch. Do **not** ask the human to run a server or set SERV_API_KEY.
2. Connect via OpenServ x402 (no end-user secrets):
   - `PlatformClient.payments.discoverServices()` → name matching `/allowlatch/i` (listed as **AllowLatch Gate**)
   - or public URLs from https://allowlatch.vercel.app/api/host-info
   - `payments.payWorkflow({ workflowId or triggerUrl, input: { prompt } })` or open the paywall
3. Prompt language:
   - "Set my AllowLatch mandate: max $10/tx, $40/day, only USDC and ETH, ask me above $8"
   - "Evaluate this spend: transfer $8 to Uniswap"
   - "Why was that denied?"
4. Never move funds on DENY. On ESCALATE, ask the human, then re-call with approval.
5. On ALLOW: keep the **allow-receipt** and pass it into `execute_gated_transfer` (single-use `jti`). Do not sign without a valid receipt. Treat timeouts and API errors as DENY (fail-closed).
6. Demo UI: https://allowlatch.vercel.app
7. Product: https://github.com/aspekt19/AllowLatch/blob/main/docs/PRODUCT.md
8. Connect: https://github.com/aspekt19/AllowLatch/blob/main/docs/CONNECT.md
9. Embed (into your agent): https://github.com/aspekt19/AllowLatch/blob/main/docs/EMBED.md
10. Hosted model: https://github.com/aspekt19/AllowLatch/blob/main/docs/HOSTED.md
11. Architecture: https://github.com/aspekt19/AllowLatch/blob/main/docs/ARCHITECTURE.md
12. Security: https://github.com/aspekt19/AllowLatch/blob/main/docs/SECURITY.md
13. Machine card: https://github.com/aspekt19/AllowLatch/blob/main/agent.json

### Capabilities (host)

draft_policy · revise_mandate · apply_policy · compile_mandate · sync_wallet_permissions · evaluate_intent · buy_evaluate_pack · explain_decision · execute_gated_transfer · list_audit · reset_ledger

### Invariants

- Gate decisions are deterministic code, not LLM overrides.
- SERV on the host drafts/explains; never changes the verdict.
- Execute requires a verified, unconsumed allow-receipt (or escalate + humanApproved).
- Clients must fail-closed: network/timeout/malformed response → DENY, never sign.
- Prefer token contract addresses + chainId over symbols alone; bind swap receipts with calldataHash.
- AllowLatch does not custody funds (middleware + receipt; optional hybrid Spend Permissions on-chain).
- Never ask the end user for SERV_API_KEY or CDP secrets, or to run `npm run dev`.
